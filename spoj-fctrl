#include <stdio.h>
int main()
{
    int T;
    scanf("%d",&T);
    while(T--)
    {
        long long inputan;
        scanf("%lld",&inputan);
        long long jawab=0;
        while(inputan)
        {
            inputan = inputan/5;
            jawab+=inputan;
        }
        printf("%lld\n",jawab);
    }
    return 0;
}
